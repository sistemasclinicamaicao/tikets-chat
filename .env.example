POSTGRES_DB=tickets_db
POSTGRES_USER=tickets_user
POSTGRES_PASSWORD=tickets_pass
POSTGRES_PORT=5432

REDIS_PASSWORD=redis_pass
REDIS_PORT=6379

# Defina una sola vez MINIO_ENDPOINT. Si el backend apunta directo a la IP pública del QNAP,
# use HTTPS; HTTP fue rechazado en las pruebas del proyecto.
MINIO_ENDPOINT=https://179.60.240.86:8010
MINIO_BUCKET=archivos_chat
MINIO_ACCESS_KEY=change_me_access_key
MINIO_SECRET_KEY=change_me_secret_key
MINIO_REGION=colombia
STORAGE_SIGNED_URL_EXPIRES_SECONDS=3600
# Temporalmente, si el certificado del QNAP no es confiable desde el servidor:
# STORAGE_TLS_REJECT_UNAUTHORIZED=false
CHAT_ATTACHMENT_IMAGE_MAX_MB=10
CHAT_ATTACHMENT_VIDEO_MAX_MB=100
CHAT_ATTACHMENT_FILE_MAX_MB=25

N8N_PORT=5678
N8N_HOST=localhost
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=admin123

MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USERNAME=tu_correo@gmail.com
MAIL_PASSWORD=tu_app_password
# Directorio de chat: máximo de empleados activos en GET /chat/users (solo si defines esta clave en apps/api/.env).
# Si no está en el archivo .env del API, se usa 100000 y se ignoran variables homónimas del sistema operativo.
# CHAT_DIRECTORY_USER_LIMIT=100000
