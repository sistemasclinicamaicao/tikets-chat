# Copia este archivo a `.env.production` (está en .gitignore).
# Vite lo carga en `npm run build` / `cap:sync:android`.
# URL HTTPS pública del API en Easypanel (Nest + Socket.IO), sin barra final.
VITE_API_ORIGIN=https://tu-dominio-api.easypanel.host
